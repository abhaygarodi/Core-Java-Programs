// Create an abstract class Shape with an abstract method double area(). 
// Then, create two subclasses, Circle and Rectangle, that extend Shape and provide implementations
//  for the area method. Write a main method to create instances of Circle and Rectangle, 
//  and display their areas.
public abstract class Shape{
	abstract double area();
}